syntax = "proto3";
package common;
import "common/common.proto";

option go_package = "gitlab.1dmy.com/ezbuy/base/dist/proto/common";

// 仅适用于小文件
message File {
	FileHeader header = 1;
	bytes data = 2;
}

message FileHeader {
	string filename = 1;
	map<string, Strings> mime = 2;
}
