syntax = "proto3";
package yqhcallback;
import "common/ezbuy.proto";
service Callback {
	option (common.service).webapi = true;
	rpc Upsert(YiqihuoGet) returns (CallbackResp);
}

// ---------------------------------------------------------------------------

message YiqihuoGet {
	string shopname =1;
	repeated string id = 2;
	string appkey =3;
	string token =4;
}
//code 0:操作成功;1:操作失败
message CallbackResp {
	int32 code = 1;
	string msg = 2;
	repeated string faildIds = 3;
}

