syntax = "proto3";
package ezseller;

message Result {
    int32 code = 1; // todo 替换为ResultCode
    string message = 2;
}

enum ResultCode {
    Ok = 0;
    Error = 1;

    InvalidOrderStatus = 112;
}
