syntax = "proto3";
package assassin;
import "assassin/assassin.proto";
import "common/ezbuy.proto";

service One688 {
    option (common.service).webapi = true;
    rpc Get(One688Get) returns (One688Resp);
}

message One688Get {
    string id = 1;
    bool notRealTime = 2;
}

message One688Resp {
    Product product = 1;
}