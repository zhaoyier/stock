syntax = "proto3";
package assassin;
import "assassin/assassin.proto";
import "common/ezbuy.proto";

service Other {
  option (common.service).webapi = true;
  rpc Get(OtherGet) returns (OtherGetResp);
}

// ---------------------------------------------------------------------------

message OtherGet {
	string url = 1;
}

message OtherGetResp {
	Product product = 1;
}

