syntax = "proto3";
package spkadmin;

import "common/ezbuy.proto";
import "common/html.proto";

// 筛选商品
service List {
  option (common.service).webapi = true;
  // 
  rpc Index(ListIndex) returns (common.Html);
}

// ---------------------------------------------------------------------------

message ListIndex {
}
