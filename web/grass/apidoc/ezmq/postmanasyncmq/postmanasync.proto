syntax = "proto3";
package postmanasyncmq;

import "common/empty.proto";
import "postman/postman.proto";

service postmanasync {
	rpc PostNotify(postman.OnceNotifyReq) returns(common.Empty);
	rpc PostGroupNotify(postman.NotifyReq) returns(common.Empty);
}

